<% content_for :title, "Edit task — #{@project.name}" %>
<div class="saas-crumb"><%= link_to "Projects", projects_path %> <span class="sep">/</span> <%= link_to @project.name, @project %> <span class="sep">/</span> <%= link_to @task.title, [@project, @task] %> <span class="sep">/</span> <span>Edit</span></div>
<%= render "form" %>
