<% content_for :title, "#{@task.title} — #{@project.name}" %>

<div class="saas-crumb"><%= link_to "Projects", projects_path %> <span class="sep">/</span> <%= link_to @project.name, @project %> <span class="sep">/</span> <span><%= truncate(@task.title, length: 40) %></span></div>

<div class="saas-detail">
  <div class="saas-detail-main">
    <%= render "header" %>
    <%= render "checklist" %>
    <%= render "comments" %>
  </div>
  <div class="saas-detail-main">
    <%= render "docs_attachments" %>
  </div>
</div>
