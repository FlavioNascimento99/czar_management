# Name of your application. Used to uniquely configure containers.
# Deploy target values come from the environment so this file is safe for git.
# Required before the first deploy:
#   export KAMAL_IMAGE="your-dockerhub-user/czar_management"  # or ghcr.io/your-user/czar_management
#   export KAMAL_SERVER_IP="203.0.113.10"                     # your VPS address
#   export KAMAL_PROXY_HOST="app.example.com"                 # public domain (proxied by Cloudflare)
#   export KAMAL_REGISTRY_USER="your-dockerhub-user"
#   export APP_HOST="app.example.com"                         # must match KAMAL_PROXY_HOST
#   export KAMAL_REGISTRY_PASSWORD="..."                       # registry token (see .kamal/secrets)
#   export SECRET_KEY_BASE="$(openssl rand -hex 64)"           # generate ONCE, store safely
service: czar_management

# Name of the container image.
image: <%= ENV.fetch("KAMAL_IMAGE") %>

# Deploy to these servers.
servers:
  web:
    - <%= ENV.fetch("KAMAL_SERVER_IP") %>
  # job:
  #   hosts:
  #     - 192.168.0.1
  #   cmd: bin/jobs

# Enable SSL auto certification via Let's Encrypt and allow for multiple apps on a single web server.
# Remove this section when using multiple web servers and ensure you terminate SSL at your load balancer.
#
# Note: If using Cloudflare, set encryption mode in SSL/TLS setting to
# "Full (strict)" to enable CF-to-app encryption. "Flexible" + Rails
# force_ssl causes an infinite redirect loop.
proxy:
  ssl: true
  host: <%= ENV.fetch("KAMAL_PROXY_HOST") %>

# Credentials for your image host.
registry:
  # Specify the registry server, if you're not using Docker Hub
  # server: registry.digitalocean.com / ghcr.io / ...
  username: <%= ENV.fetch("KAMAL_REGISTRY_USER") %>

  # Always use an access token rather than real password when possible.
  password:
    - KAMAL_REGISTRY_PASSWORD

# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    - SECRET_KEY_BASE
  clear:
    # Public hostname, also used for Host authorization and mailer links.
    APP_HOST: <%= ENV.fetch("APP_HOST") %>
    # Run the Solid Queue Supervisor inside the web server's Puma process to do jobs.
    # When you start using multiple servers, you should split out job processing to a dedicated machine.
    SOLID_QUEUE_IN_PUMA: true

    # Set number of processes dedicated to Solid Queue (default: 1)
    # JOB_CONCURRENCY: 3

    # Set number of cores available to the application on each server (default: 1).
    # WEB_CONCURRENCY: 2

    # Match this to any external database server to configure Active Record correctly
    # Use czar_management-db for a db accessory server on same machine via local kamal docker network.
    # DB_HOST: 192.168.0.2

    # Log everything from Rails
    # RAILS_LOG_LEVEL: debug

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole"


# Persistent volume for local Active Storage files. The production database is
# Postgres (Supabase) per config/database.yml — not SQLite — so only
# /rails/storage needs persistence here.
volumes:
  - "czar_management_storage:/rails/storage"


# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets

# Configure the image builder.
builder:
  arch: amd64

  # # Build image via remote server (useful for faster amd64 builds on arm64 computers)
  # remote: ssh://docker@docker-builder-server
  #
  # # Pass arguments and secrets to the Docker build process
  # args:
  #   RUBY_VERSION: 3.2.2
  # secrets:
  #   - GITHUB_TOKEN
  #   - RAILS_MASTER_KEY

# Use a different ssh user than root
# ssh:
#   user: app

# Use accessory services (secrets come from .kamal/secrets).
# accessories:
#   db:
#     image: mysql:8.0
#     host: 192.168.0.2
#     # Change to 3306 to expose port to the world instead of just local network.
#     port: "127.0.0.1:3306:3306"
#     env:
#       clear:
#         MYSQL_ROOT_HOST: '%'
#       secret:
#         - MYSQL_ROOT_PASSWORD
#     files:
#       - config/mysql/production.cnf:/etc/mysql/my.cnf
#       - db/production.sql:/docker-entrypoint-initdb.d/setup.sql
#     directories:
#       - data:/var/lib/mysql
#   redis:
#     image: redis:7.0
#     host: 192.168.0.2
#     port: 6379
#     directories:
#       - data:/data
